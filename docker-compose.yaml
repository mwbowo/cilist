version: '3.8'

services:
  database:
    container_name: cilist-db
    image: mwbowo/cilist-db:latest

  backend:
    image: mwbowo/cilist-be:latest
    ports:
      - "5000:5000"
    depends_on:
      - database
    environment:
      - BASE_URL_PORT=5000
      - MYSQL_ROOT_PASSWORD=s3k0l4hd3v0p5
      - DATABASE_USERNAME=people
      - DATABASE_PASSWORD=people
      - DATABASE_DATABASE=people
      - DATABASE_HOST=database
      - DATABASE_PORT=3306
  
  frontend:
    image: mwbowo/cilist-fe:latest
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_BACKEND_URL=http://34.229.253.236:5000
